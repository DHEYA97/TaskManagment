namespace TaskManagment.Core.Service
{
    public interface IEmailSenderService
    {
        //Task<string?> SendRegistrationEmailInBackgroundAsync(BaseTemplateViewModel newRegister, string qrCodePath, DateTime? sendDate = null);
    }
}
